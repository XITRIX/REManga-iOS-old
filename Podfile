# Uncomment the next line to define a global platform for your project
source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'
use_frameworks!

target 'REManga' do
  # Pods for REManga
  pod 'Bond'
  pod 'Alamofire', '~> 5.2'
  pod 'Kingfisher', '~> 6.0'
  pod "TTGTagCollectionView"
  pod 'MarqueeLabel'
  pod "PromiseKit", "~> 6.8"

  target 'REMangaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'REMangaUITests' do
    # Pods for testing
  end

end
